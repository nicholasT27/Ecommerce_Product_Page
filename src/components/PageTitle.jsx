export default function PageTitle({ eyebrow, title, children, centered = false }) { return <div className={`max-w-[1110px] mx-auto px-6 pt-10 lg:pt-16 ${centered ? 'text-center' : ''}`}>{eyebrow && <p className="text-orange text-sm font-bold tracking-widest uppercase">{eyebrow}</p>}<h1 className="text-3xl lg:text-5xl font-bold mt-2">{title}</h1>{children && <p className={`text-dgblue mt-3 max-w-2xl ${centered ? 'mx-auto' : ''}`}>{children}</p>}</div>; }
